name: Build and Release

on:
  push:
    branches:
      - server-client
  workflow_dispatch:

jobs:
  windows-client-build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.11.4"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r client/requirements_windows.txt

      - name: Build application
        run: python client/setup.py build
        shell: cmd

      - name: Archive build output
        uses: actions/upload-artifact@v2
        with:
          name: build-output-windows
          path: build/exe.*

  windows-client-prepare-release:
    needs: windows-client-build
    runs-on: windows-latest
    outputs:
      release_url: ${{ steps.create_windows_client_release.outputs.upload_url }}
    steps:
      - name: Set release SHA
        run: |
          $sha = "${{ github.sha }}"
          echo "SHA=$sha" >> $env:GITHUB_ENV
        shell: pwsh

      - name: Create Release
        id: create_windows_client_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: windows-client-${{ env.SHA }}
          release_name: Release Client Windows
          draft: false
          prerelease: false

      - name: Output release URL
        run: echo "release_url=${{ steps.create_windows_client_release.outputs.upload_url }}" >> $GITHUB_OUTPUT

  windows-client-package-and-upload:
    needs: [windows-client-build, windows-client-prepare-release]
    runs-on: windows-latest
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: build-output-windows

      - name: Zip the build directory
        run: Compress-Archive -Path exe.win-amd64-3.11 -DestinationPath windows-client.zip
        shell: pwsh

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_windows_client_release.outputs.upload_url }}
          asset_path: ./windows-client.zip
          asset_name: windows-client.zip
          asset_content_type: application/zip
